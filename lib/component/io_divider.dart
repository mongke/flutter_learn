import 'package:flutter/material.dart';

class IoDivider extends StatelessWidget {
  const IoDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 1,
      width: double.infinity,
      color: Colors.grey[300],
    );
  }
}
